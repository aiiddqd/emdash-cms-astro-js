import { Button } from "flowbite-react";
import * as React from 'react';

export default function Component() {
  return (
    <Button.Group>
      <Button color="gray">Profile1</Button>
      <Button color="gray">Settings</Button>
      <Button color="gray">Messages</Button>
    </Button.Group>
  );
}
